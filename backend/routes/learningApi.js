// learningApi.js — retired. The Personalized Learning Path Agent was
// removed from the product. Route file kept as a stub so any lingering
// import doesn't crash; not mounted in server.js anymore.

const express = require('express');
const router = express.Router();

module.exports = router;
